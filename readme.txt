AUTOMATION SCRIPT FOR TEST CASE:

Programming language used : Java, Selenium Webdriver, TestNG framework for assert,Maven dependancy for JAR files.
IDE Used: eclipseIDE

Instructions: Chromedriver(version - 95.0.4638.69). Chromedriver version used while executing (95.0.4638.69).Chromedriver is present within the workspace.

The executable files are present within src -> main -> java.

Unzip the given file.

Open Eclipse IDE. Click file --> Import.

Click maven --> Existing maven project --> Select the root folder(task1)

The program should be run from verification as it is the main class for the test script.